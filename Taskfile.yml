# SPDX-License-Identifier: BSD-3-Clause
# Copyright 2026 Intel Corporation
#
# Entry points that CI and developers share, so a local run and a runner run
# invoke exactly the same command. Install with:
#   go install github.com/go-task/task/v3/cmd/task@latest
# or see https://taskfile.dev/installation/
#
#   task --list

version: '3'

vars:
  EBPF_SCRIPT: '{{.TASKFILE_DIR}}/script/build_ebpf_xdp.sh'
  CI_LOCAL: '{{.TASKFILE_DIR}}/.github/ci-local'
  CI_SCRIPTS: '{{.TASKFILE_DIR}}/.github/scripts/ci'

tasks:
  default:
    cmds:
      - task --list
    silent: true

  ebpf:check:
    desc: Verify eBPF/XDP prerequisites and pinned versions; fail if missing
    summary: |
      Runs script/build_ebpf_xdp.sh --check --strict.

      Meant to run first in any job that will later touch DPDK or AF_XDP, so
      the job dies in seconds instead of failing inside a pkg-config call
      minutes later. Vars: MODE (build|runtime|all), REQUIRE_XDP (0|1).
    vars:
      MODE: '{{.MODE | default "all"}}'
      REQUIRE_XDP: '{{.REQUIRE_XDP | default "0"}}'
    cmds:
      - >-
        bash {{.EBPF_SCRIPT}} --check --mode {{.MODE}} --strict
        {{if eq .REQUIRE_XDP "1"}}--require-xdp{{end}}

  ebpf:report:
    desc: Same checks as ebpf:check, but never fails the caller
    cmds:
      - bash {{.EBPF_SCRIPT}} --check --mode all

  ebpf:install:
    desc: Build and install xdp-tools + libbpf at the versions in versions.env
    cmds:
      - bash {{.EBPF_SCRIPT}}

  ci:hash-dependencies:
    desc: Compute deterministic dependency source hashes
    cmds:
      - bash {{.TASKFILE_DIR}}/script/hash_sources.sh {{.CLI_ARGS}}

  ci:cache-keys:
    desc: Emit exact dependency cache keys for the current runner
    cmds:
      - bash {{.CI_SCRIPTS}}/cache-keys.sh

  ci:evaluate-caches:
    desc: Validate restored caches and export build-miss state
    cmds:
      - bash {{.CI_SCRIPTS}}/evaluate-caches.sh

  ci:validate-cache:
    desc: Structurally validate one restored dependency cache
    requires:
      vars: [COMPONENT]
    cmds:
      - bash {{.CI_SCRIPTS}}/validate-cache.sh {{.COMPONENT}}

  ci:build-jpegxs:
    desc: Build the relocatable JPEG XS runtime and bridge bundle
    cmds:
      - bash {{.CI_SCRIPTS}}/build-jpegxs.sh

  ci:validate-jpegxs:
    desc: Validate the relocatable JPEG XS bundle
    cmds:
      - bash {{.CI_SCRIPTS}}/validate-jpegxs.sh

  ci:build-ice:
    desc: Build a patched ICE module artifact for the running kernel ABI
    cmds:
      - bash {{.TASKFILE_DIR}}/script/build_ice_driver.sh

  ci:validate-ice:
    desc: Validate the cached ICE module against the running kernel ABI
    cmds:
      - bash {{.CI_SCRIPTS}}/validate-ice.sh

  ci:activate-ice:
    desc: Idempotently activate the validated ICE module artifact
    cmds:
      - bash {{.CI_SCRIPTS}}/activate-ice.sh {{.CLI_ARGS}}

  ci:build-dependencies:
    desc: Build dependency cache misses selected by CI_BUILD_ variables
    cmds:
      - bash {{.CI_SCRIPTS}}/build-dependencies.sh

  ci:validate-dependencies:
    desc: Structurally validate all dependency install trees
    cmds:
      - bash {{.CI_SCRIPTS}}/validate-dependencies.sh

  ci:test-dependencies:
    desc: Run focused prebuilt dependency contract and behavior tests
    cmds:
      - bash {{.CI_LOCAL}}/tests/test_prebuilt_dependencies.sh
      - bash {{.CI_LOCAL}}/tests/test_dependency_behavior.sh
      - python3 {{.CI_LOCAL}}/tests/test_yaml_migration.py
      - python3 {{.TASKFILE_DIR}}/.github/mcp/test_mtl_ci_mcp_server.py

  ci:check-yaml:
    desc: Reject inline programs in active workflow and composite YAML
    cmds:
      - bash {{.CI_SCRIPTS}}/check-yaml-policy.sh

  ci:validation:
    desc: Run one focused validation workflow operation
    cmds:
      - bash {{.CI_SCRIPTS}}/validation-workflow.sh {{.CLI_ARGS}}

  ci:pytest-setup:
    desc: Prepare acceptance-test dependencies, session, NIC, or configuration
    cmds:
      - bash {{.CI_SCRIPTS}}/pytest-setup.sh {{.CLI_ARGS}}

  ci:pytest-run:
    desc: Run one acceptance-test workflow variant
    cmds:
      - bash {{.CI_SCRIPTS}}/pytest-run.sh {{.CLI_ARGS}}

  ci:report:
    desc: Collect or transform CI test reports
    cmds:
      - bash {{.CI_SCRIPTS}}/reports.sh {{.CLI_ARGS}}

  ci:gtest-nightly:
    desc: Run the nightly bare-metal gtest harness
    cmds:
      - sudo -E {{.TASKFILE_DIR}}/.github/scripts/gtest.sh

  ci:workflow:
    desc: Run one workflow orchestration helper
    cmds:
      - bash {{.CI_SCRIPTS}}/workflow-ops.sh {{.CLI_ARGS}}

  ci:build-action:
    desc: Build MTL locally and optionally on a remote host
    cmds:
      - bash {{.CI_SCRIPTS}}/build-action.sh

  ci:msys2:
    desc: Run one Windows MSYS2 build operation
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - bash .github/scripts/ci/msys2.sh {{.CLI_ARGS}}

  ci:configure-host:
    desc: Configure cached dependency paths and plugin registry on a test host
    cmds:
      - bash {{.CI_SCRIPTS}}/configure-host.sh {{.CLI_ARGS}}

  ci:test-pr:
    desc: Run the whole PR pipeline locally (build + pr-gate + validate-host matrix)
    cmds:
      - '{{.CI_LOCAL}}/test-pr-locally.sh {{.CLI_ARGS}}'

  ci:build:
    desc: Run the build workflow job locally in a container
    cmds:
      - '{{.CI_LOCAL}}/run-job.sh build {{.CLI_ARGS}}'

  ci:validate-host:
    desc: Run the validate-host action locally against a simulated bare-metal runner
    vars:
      NIC: '{{.NIC | default "e810"}}'
    cmds:
      - '{{.CI_LOCAL}}/run-job.sh validate-host --nic {{.NIC}} {{.CLI_ARGS}}'
