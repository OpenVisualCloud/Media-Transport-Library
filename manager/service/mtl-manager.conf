# SPDX-License-Identifier: BSD-3-Clause
# Copyright 2023 Intel Corporation
#
# Make the socket directory of the system MTL Manager at boot, so a manager
# that a user starts by hand finds it there. The service makes the same
# directory with RuntimeDirectory=, so this rule only helps a manual run.
d /run/imtl 0755 root root -
