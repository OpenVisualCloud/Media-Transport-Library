# Paths that affect the patched ICE+IAVF module artifact -- both drivers ship as
# one cached package, so a change to either one's patches must rebuild it.
.github/scripts/ci/validate-ice.sh
.github/scripts/setup_environment.sh
patches/ice_drv/
patches/iavf_drv/
script/build_drivers.sh
script/hash_sources_ice.env
versions.env