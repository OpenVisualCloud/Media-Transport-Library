project('DPDK MediaStreamer sample APPs', 'c',
                default_options: ['c_std=gnu11', 'buildtype=release'],
		version: '1.0',)

common_app_src = []
include_dir = []

subdir('common')

subdir('rxtx_app')